# Production environment overrides
# These values are committed as safe defaults. Secrets are set in Vercel env vars.

# Pharmacy AI Brain — disabled in production by default.
# To pilot: set VITE_BRAIN_PHARMACY=true in Vercel Dashboard → Project → Environment Variables.
VITE_BRAIN_PHARMACY=false
